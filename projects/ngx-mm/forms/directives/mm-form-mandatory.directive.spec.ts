import { MmFormMandatoryDirective } from './mm-form-mandatory.directive';

describe('MmFormMandatoryDirective', () => {
  it('should create an instance', () => {
    const directive = new MmFormMandatoryDirective();
    expect(directive).toBeTruthy();
  });
});
