import { FormHelperDirective } from './form-helper.directive';

describe('FormHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new FormHelperDirective();
    expect(directive).toBeTruthy();
  });
});
